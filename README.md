# ALZEYE 

* create virtual env
```powershell
python -m venv venv
.\venv\Scripts\Activate.ps1

```

* run the requirements file using : 
```powershell
pip install -r requirements.txt

```
* install face recognition module
```powershell
pip install face_recognition==1.3.0 --no-deps
```


